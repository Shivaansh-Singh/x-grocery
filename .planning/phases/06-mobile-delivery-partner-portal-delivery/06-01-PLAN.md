---
phase: 06
plan: 01
title: Rider Orders API, Profile Selector & Delivery Task Card Component
wave: 1
mode: mvp
requirements: [DEL-01]
files_modified: [
  "src/app/api/delivery/orders/route.ts",
  "src/components/delivery/RiderProfileSelector.tsx",
  "src/components/delivery/DeliveryTaskCard.tsx"
]
---

# Plan 06-01: Rider Orders API, Profile Selector & Delivery Task Card Component

## Executive Summary

Build the rider assigned orders API (`GET /api/delivery/orders`), quick rider profile selector bar (`RiderProfileSelector`), and mobile delivery task card component (`DeliveryTaskCard`) displaying student contact details, direct `'Call Student 📞'` button (`tel:${phone}`), off-campus residential address, item checklist, and payment mode badge.

---

## Tasks

<task_block>
### Task 06-01-01: Create Rider Assigned Orders API Route Handler (`src/app/api/delivery/orders/route.ts`)
**Goal**: Build a server route handler to fetch active and completed delivery tasks assigned to the active rider.
**Files**: `src/app/api/delivery/orders/route.ts`
**Requirements**: DEL-01
**Threat Ref**: T-06-01 (Unauthorized order access)
**Secure Behavior**: Filter orders by assigned delivery partner ID or store.

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 06-01-02: Create Rider Profile Selector Component (`src/components/delivery/RiderProfileSelector.tsx`)
**Goal**: Build a top profile selector bar allowing quick switching between registered Store X delivery staff riders (Ramesh Kumar, Suresh Singh).
**Files**: `src/components/delivery/RiderProfileSelector.tsx`
**Requirements**: DEL-01

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 06-01-03: Create Mobile Delivery Task Card Component (`src/components/delivery/DeliveryTaskCard.tsx`)
**Goal**: Build a touch-optimized card displaying Order #, student name, direct phone call trigger, off-campus delivery address, item checklist preview, and payment badge.
**Files**: `src/components/delivery/DeliveryTaskCard.tsx`
**Requirements**: DEL-01

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

---

## Verification Plan

### Automated Checks
- `npx tsc --noEmit` — Type check across delivery components
- `npm run lint` — ESLint validation
