---
phase: 04
plan: 02
title: Live Tracking API, 6-Step Vertical Timeline, Auto-Polling Sync & Rider Contact Card
wave: 2
mode: mvp
requirements: [ORD-03]
files_modified: [
  "src/app/api/orders/[id]/route.ts",
  "src/components/orders/OrderTrackingTimeline.tsx",
  "src/components/orders/RiderContactCard.tsx",
  "src/app/orders/[id]/page.tsx"
]
---

# Plan 04-02: Live Tracking API, 6-Step Vertical Timeline, Auto-Polling Sync & Rider Contact Card

## Executive Summary

Build the order detail tracking API (`GET /api/orders/[id]`), 6-step vertical progress timeline UI, 3-5s auto-polling realtime status sync, rider contact card (`Rider Name` + `'Call Rider'` button), and live customer tracking page (`/orders/[id]`).

---

## Tasks

<task_block>
### Task 04-02-01: Create Order Tracking Detail API (`src/app/api/orders/[id]/route.ts`)
**Goal**: Build a server route handler returning full order details, items, store info, and assigned delivery partner.
**Files**: `src/app/api/orders/[id]/route.ts`
**Requirements**: ORD-03
**Threat Ref**: T-04-02 (Unauthorized order tracking leakage)
**Secure Behavior**: Return order details matching order ID or orderNumber code.

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 04-02-02: Create 6-Step Vertical Progress Timeline (`src/components/orders/OrderTrackingTimeline.tsx`)
**Goal**: Build a vertical timeline highlighting the 6 delivery stages (`Placed` → `Accepted` → `Preparing` → `Assigned` → `Out for Delivery` → `Delivered`) with active green indicators.
**Files**: `src/components/orders/OrderTrackingTimeline.tsx`
**Requirements**: ORD-03

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 04-02-03: Create Rider Contact Card Component (`src/components/orders/RiderContactCard.tsx`)
**Goal**: Build a rider information card displaying assigned rider name, Store X Staff badge, and clickable direct `'Call Delivery Rider'` button (`tel:${riderPhone}`).
**Files**: `src/components/orders/RiderContactCard.tsx`
**Requirements**: ORD-03

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 04-02-04: Construct Live Customer Order Tracking Page (`src/app/orders/[id]/page.tsx`)
**Goal**: Assemble 3-5s auto-polling sync, 6-step timeline, rider contact card, delivery address summary, and itemized bill on `/orders/[id]`.
**Files**: `src/app/orders/[id]/page.tsx`
**Requirements**: ORD-03

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

---

## Verification Plan

### Automated Checks
- `npx tsc --noEmit` — Type check across tracking components
- `npm run lint` — ESLint validation
