---
phase: 05
plan: 01
title: Admin Catalog CRUD, Inline Stock Steppers & Inventory Audit Logging
wave: 1
mode: mvp
requirements: [ADMIN-01, ADMIN-02]
files_modified: [
  "src/app/api/admin/products/route.ts",
  "src/app/api/admin/products/[id]/route.ts",
  "src/components/admin/ProductEditModal.tsx",
  "src/components/admin/CreateProductModal.tsx",
  "src/app/admin/products/page.tsx"
]
---

# Plan 05-01: Admin Catalog CRUD, Inline Stock Steppers & Inventory Audit Logging

## Executive Summary

Build the admin product management route handlers (`/api/admin/products` and `/api/admin/products/[id]`), inline `+` / `-` stock stepper controls, product price & detail edit modal, product creation modal, and automated `InventoryLog` audit entry generation.

---

## Tasks

<task_block>
### Task 05-01-01: Create Admin Product CRUD & Stock API Route Handlers (`src/app/api/admin/products/route.ts` & `src/app/api/admin/products/[id]/route.ts`)
**Goal**: Build server route handlers to list products, create new products, update prices/details, and perform stock overrides with automatic `InventoryLog` audit creation.
**Files**: `src/app/api/admin/products/route.ts`, `src/app/api/admin/products/[id]/route.ts`
**Requirements**: ADMIN-01, ADMIN-02
**Threat Ref**: T-05-01 (Unauthorized catalog manipulation)
**Secure Behavior**: Verify target store exists and validate payload numeric constraints.

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 05-01-02: Create Product Edit & Create Modals (`src/components/admin/ProductEditModal.tsx` & `src/components/admin/CreateProductModal.tsx`)
**Goal**: Build interactive modal components for editing existing product price/unit/description/image and creating new products.
**Files**: `src/components/admin/ProductEditModal.tsx`, `src/components/admin/CreateProductModal.tsx`
**Requirements**: ADMIN-01

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 05-01-03: Construct Admin Product Catalog Manager Page (`src/app/admin/products/page.tsx`)
**Goal**: Build `/admin/products` page rendering product table with category filtering, inline `+` / `-` stock steppers, active toggle switch, and modal triggers.
**Files**: `src/app/admin/products/page.tsx`
**Requirements**: ADMIN-01, ADMIN-02

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

---

## Verification Plan

### Automated Checks
- `npx tsc --noEmit` — Type check across catalog components
- `npm run lint` — ESLint validation
