---
phase: 03
plan: 02
title: Address API, Off-Campus Hub Selector & 1-Page Checkout Flow (COD & UPI)
wave: 2
mode: mvp
requirements: [CART-02, CART-03]
files_modified: [
  "src/app/api/addresses/route.ts",
  "src/components/checkout/OffCampusAddressForm.tsx",
  "src/components/checkout/PaymentMethodSelector.tsx",
  "src/app/cart/checkout/page.tsx"
]
---

# Plan 03-02: Address API, Off-Campus Hub Selector & 1-Page Checkout Flow (COD & UPI)

## Executive Summary

Build the customer delivery address API (`POST /api/addresses`), structured off-campus address selector with hub presets (`Royal City`, `Kotri Kalan`, `Main Gate`), prominent off-campus service area warning banner, and 1-page checkout view supporting Cash on Delivery (COD) and UPI on Delivery (QR Code).

---

## Tasks

<task_block>
### Task 03-02-01: Create Customer Address API Endpoint (`src/app/api/addresses/route.ts`)
**Goal**: Build a server-side route handler to create and fetch customer delivery addresses.
**Files**: `src/app/api/addresses/route.ts`
**Requirements**: CART-02
**Threat Ref**: T-03-02 (Address injection / malicious input payload)
**Secure Behavior**: Sanitize and validate input fields (`buildingColony`, `flatRoomNo`, `phone`).

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 03-02-02: Create Off-Campus Housing Address Form Component (`src/components/checkout/OffCampusAddressForm.tsx`)
**Goal**: Build an address entry component with quick hub presets (`Royal City`, `Kotri Kalan`, `Main Gate`), structured fields, and a prominent Off-Campus Service Warning Banner.
**Files**: `src/components/checkout/OffCampusAddressForm.tsx`
**Requirements**: CART-02

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 03-02-03: Create Payment Method Radio Selector (`src/components/checkout/PaymentMethodSelector.tsx`)
**Goal**: Build interactive radio selection cards for `Cash on Delivery (COD)` and `Pay via UPI on Delivery (Scan QR Code at Doorstep)`.
**Files**: `src/components/checkout/PaymentMethodSelector.tsx`
**Requirements**: CART-03

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

<task_block>
### Task 03-02-04: Construct 1-Page Checkout Flow (`src/app/cart/checkout/page.tsx`)
**Goal**: Assemble address selection, payment method cards, itemized bill breakdown, and order submission button on `/cart/checkout`.
**Files**: `src/app/cart/checkout/page.tsx`
**Requirements**: CART-02, CART-03

**Verification**: `npm run lint && npx tsc --noEmit`
</task_block>

---

## Verification Plan

### Automated Checks
- `npx tsc --noEmit` — Type check across checkout components
- `npm run lint` — ESLint validation
